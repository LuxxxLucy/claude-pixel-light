.DS_Store

# Build outputs, regenerated by ./build.sh and by the Pages workflow.
/index.html
/assets/lightbar.svg

# vendor/dual-typst is a local symlink to the canonical dual-tufte-typst
# checkout; CI clones the library instead (see .github/workflows/pages.yml).
/vendor/
